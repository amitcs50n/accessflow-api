services:
  postgres:
    image: postgres:16
    container_name: accessflow_postgres
    environment:
      POSTGRES_USER: accessflow
      POSTGRES_PASSWORD: accessflow
      POSTGRES_DB: accessflow_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
